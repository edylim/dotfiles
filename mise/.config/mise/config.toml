[tools]
# Track the current LTS/stable lines (matches install.sh's `mise use --global
# node@lts`); avoids pinning an exact patch that every machine must install.
node = "lts"
python = "3"

[settings]
experimental = true
