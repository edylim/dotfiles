# edit-lock policy — one authoring host per repo.
# Format:  <repo-name (origin URL basename, no .git)>   <allowed host (hostname -s)>
# Any repo NOT listed here is unrestricted. Bypass once with --no-verify.
#
# Hosts:  balthazar = this Mac (.125)   kira = the kira box (.130)   kira-ms01 = ms01 VM (.100)

dotfiles      balthazar
kira          kira
kiracode      kira
kira-studio   kira
anima         kira
